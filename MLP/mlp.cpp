#include <iostream>
#include <vector>

using namespace std;

class MLP{
    public:
        bool train(vector<double> inputs)
        {
            return true;
        }

    private:
        vector<double> inputs;
        vector<double> weights;
};